# Smartness · Shorts Lab
Public dashboard for the @Smartness Shorts experiment tracker. `experiments.json` is the shared store the app reads/writes (and the AI ingests). Contains ONLY the dashboard, opening filmstrips, and that store — no research, transcripts, or strategy docs.
